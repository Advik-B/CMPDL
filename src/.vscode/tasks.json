{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build EXE",
            "type": "shell",
            "command": "${cwd}\\venv\\Scripts\\Activate.ps1 | ${cwd}\\venv\\Scripts\\python.exe -m pip install -r ${cwd}\\requirements.txt | ${cwd}\\venv\\Scripts\\python.exe setup.py build",
            "problemMatcher": [],
            "group": {
                
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}